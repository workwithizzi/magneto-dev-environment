#!/bin/bash
# Run a command as app-user with tty
# Previously called `cli`
source bin/utils
CONTAINER="$CONT_MAGENTO"

[ -z "$1" ] && print_error "Please specify a CLI command (ex. ls)" && exit

docker-compose exec "$CONTAINER" "$@"

# docker exec "$CONTAINER" "$@"