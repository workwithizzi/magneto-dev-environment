#!/bin/bash
# Creates backup of mysql db in the 'db/' directory

TIME="$(date +'%y%m%d_%H%M%S')"

if [ ! -d "db" ]; then
	mkdir -p "db"
fi

bin/n98-magerun2 db:dump --stdout > "db/${TIME}_magento.sql"
