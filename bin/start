#!/bin/bash
# Build any images and start any containers
source bin/utils
IMAGE="$CONT_MAGENTO"

print_doing "Starting containers"
docker-compose -f docker-compose.yml up -d --remove-orphans

# docker build -t yisraelgrimes/"$IMAGE":latest .
# docker container run -d --name "$IMAGE" yisraelgrimes/"$IMAGE":latest

if [ -z "$1" ]; then
	bin/info
fi