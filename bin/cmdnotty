#!/bin/bash
# Originally called 'clinotty'
# Run a command in the container without TTY (for non-interactive commands)
[ -z "$1" ] && echo "Please specify a CLI command (ex. ls)" && exit
docker-compose exec -T phpfpm "$@"
