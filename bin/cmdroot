#!/bin/bash
# Originally called 'root'
# Run a command in the container as 'root' while keeping TTY abilities (for interactive process)
[ -z "$1" ] && echo "Please specify a CLI command (ex. ls)" && exit
docker-compose exec -u root phpfpm "$@"
