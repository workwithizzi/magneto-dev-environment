#!/bin/bash
# Open an interactive terminal in container as root-user
source bin/utils
CONTAINER="$CONT_MAGENTO"

# docker-compose exec -u root "$CONTAINER" bash
docker exec -it -u root "$CONTAINER" bash