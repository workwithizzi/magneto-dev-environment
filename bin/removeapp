#!/bin/bash
# Stops any running containers
source bin/utils
IMAGE="$CONT_MAGENTO"

print_doing "Stopping $IMAGE"
# docker-compose -f docker-compose.yml stop

# docker container stop "$IMAGE"
# docker container rm "$IMAGE"

docker image rm "yisraelgrimes/$IMAGE"