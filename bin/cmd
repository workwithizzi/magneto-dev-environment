#!/bin/bash
# Originally called 'cli'
# Run a command in the container while keeping TTY abilities (for interactive process)
[ -z "$1" ] && echo "Please specify a CLI command (ex. ls)" && exit
docker-compose exec phpfpm "$@"
