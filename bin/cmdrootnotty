#!/bin/bash
# Originally called 'rootnotty'
# Run a command in the container as 'root' without TTY (for non-interactive commands)
[ -z "$1" ] && echo "Please specify a CLI command (ex. ls)" && exit
docker-compose exec -u root -T phpfpm "$@"
